const { Pool } = require(\"pg\"); const pool = new Pool({ user: \"postgres\", host: \"localhost\", database: \"Fitness_Routines\", password: \"2Comidas.\", port: 5432 }); async function checkTable() { try { const result = await pool.query(\"SELECT column_name, data_type FROM information_schema.columns WHERE table_name = \'usuarios\'\"); console.log(\"Estructura de la tabla usuarios:\"); console.log(result.rows); process.exit(0); } catch (err) { console.error(\"Error:\", err); process.exit(1); } } checkTable();
